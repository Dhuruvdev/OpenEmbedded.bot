'use strict';

/**
 * start.js — Entry point wrapper (delegates to src/index.js).
 * Kept for backward compatibility. Running `node start.js` is identical
 * to `node src/index.js`.
 */
require('./src/index.js');
